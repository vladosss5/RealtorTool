<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:pages="clr-namespace:RealtorTool.Desktop.ViewModels.Pages"
             xmlns:converters="clr-namespace:RealtorTool.Desktop.Converters"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="RealtorTool.Desktop.Views.Pages.EmployeeDetailView"
             x:DataType="pages:EmployeeDetailViewModel">
    
    <UserControl.Resources>
        <converters:DateTimeFormatConverter x:Key="DateTimeFormatConverter"/>
        <converters:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </UserControl.Resources>
   
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Background="#99dddddd" 
                 Margin="5">
        <Border MaxWidth="200" MaxHeight="200" 
                Background="#F5F5F5" CornerRadius="20"
                Margin="10">
            <Panel>
                <Border Background="LightGray"
                        CornerRadius="50" Margin="2">
                    <TextBlock Text="👱‍♂️"
                               FontSize="50"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                                    
                <Border ClipToBounds="True" CornerRadius="20">
                    <Image Source="{Binding Employee.Photo.FileData, Converter={StaticResource ByteArrayToImageConverter}}"
                           Stretch="UniformToFill"
                           IsVisible="{Binding Employee.HasPhoto}"/>
                </Border>
            </Panel>
        </Border>
        <StackPanel Margin="10">
            <TextBlock Text="{Binding Employee.FullName}" FontWeight="Bold" FontSize="25"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Роль: "
                           HorizontalAlignment="Left" 
                           Margin="5,0,0,0"
                           FontSize="20"/>
                <TextBlock Text="{Binding Employee.Role.Value}"
                           HorizontalAlignment="Left" 
                           FontSize="20"/>
            </StackPanel>
                                
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Последний вход: "
                           HorizontalAlignment="Left" 
                           Margin="5,0,0,0"
                           FontSize="20"/>
                <TextBlock Text="{Binding Employee.LastAuthentication, Converter={StaticResource DateTimeFormatConverter}}"
                           HorizontalAlignment="Left" 
                           FontSize="20"/>
            </StackPanel>
            
            <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                <Button Content="Сбросить пароль" Command="{Binding ResetPasswordCommand}" Margin="5"/>
                <Button Content="Уволить" Command="{Binding FireCommand}" Margin="5" IsVisible="{Binding CanFire}"/>
                <Button Content="Нанять обратно" Command="{Binding RecoverCommand}" Margin="5" IsVisible="{Binding CanRecover}"/>
                <Button Content="Удалить" Command="{Binding DeleteCommand}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
