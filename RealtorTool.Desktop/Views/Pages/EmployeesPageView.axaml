<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:pages="clr-namespace:RealtorTool.Desktop.ViewModels.Pages"
             xmlns:converters="clr-namespace:RealtorTool.Desktop.Converters"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="RealtorTool.Desktop.Views.Pages.EmployeesPageView"
             x:DataType="pages:EmployeesPageViewModel">

    <UserControl.Resources>
        <converters:DateTimeFormatConverter x:Key="DateTimeFormatConverter"/>
        <converters:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </UserControl.Resources>
    
    <Grid ColumnDefinitions="*, *" Margin="10">
        <!-- Список сотрудников -->
        <Border Grid.Column="0" Background="#f5f5f5" 
                Margin="10" Padding="20" CornerRadius="10"
                MaxHeight="{Binding RelativeSource={RelativeSource AncestorType=ScrollViewer}, Path=Bounds.Height}">
            <ListBox ItemsSource="{Binding Employees}" 
                     SelectedItem="{Binding SelectedEmployee}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     Background="Transparent"
                     BorderThickness="0" 
                     VerticalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#f5f5f5" CornerRadius="5">
                            <Grid ColumnDefinitions="80,Auto,Auto" 
                                  RowDefinitions="Auto,Auto,Auto" 
                                  ColumnSpacing="15" Margin="10">
                                <!-- Аватар сотрудника -->
                                <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="3"
                                        Width="70" Height="70" Background="#f5f5f5"
                                        CornerRadius="35">
                                    <Panel>
                                        <Border Background="LightGray"
                                                CornerRadius="50" Margin="2">
                                            <TextBlock Text="👱‍♂️"
                                                       FontSize="24"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                    
                                        <!-- Реальное фото -->
                                        <Border ClipToBounds="True" CornerRadius="100">
                                            <Image Source="{Binding Photo.FileData, Converter={StaticResource ByteArrayToImageConverter}}"
                                                   Stretch="UniformToFill"
                                                   IsVisible="{Binding HasPhoto}"/>
                                        </Border>
                                    </Panel>
                                </Border>
                                
                                <TextBlock Grid.Column="2" Grid.Row="0" 
                                           Text="{Binding FullName}"
                                           HorizontalAlignment="Left" 
                                           Margin="5"
                                           FontSize="16"/>
                                
                                <StackPanel Grid.Column="2" Grid.Row="1" 
                                            Orientation="Horizontal">
                                    <TextBlock Text="Роль: "
                                               HorizontalAlignment="Left" 
                                               Margin="5,0,0,0"
                                               FontSize="14"/>
                                    <TextBlock Text="{Binding Role.Value}"
                                               HorizontalAlignment="Left" 
                                               FontSize="14"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2" Grid.Row="2" 
                                            Orientation="Horizontal">
                                    <TextBlock Text="Последний вход: "
                                               HorizontalAlignment="Left" 
                                               Margin="5,0,0,0"
                                               FontSize="14"/>
                                    <TextBlock Text="{Binding LastAuthentication, Converter={StaticResource DateTimeFormatConverter}}"
                                               HorizontalAlignment="Left" 
                                               FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        
        <ScrollViewer Grid.Column="1">
            <Border Background="#f5f5f5" Margin="10" Padding="20" CornerRadius="10">
                <Grid ColumnDefinitions="Auto, *" RowDefinitions="Auto, Auto">
                    <StackPanel Grid.Column="0" Grid.Row="0">
                        <Button Command="{Binding SelectImageCommand}" 
                                CornerRadius="50" Height="200" Width="200"
                                Background="Transparent" Margin="10">
                            <Panel>
                                <Border Background="LightGray"
                                        CornerRadius="50" Margin="0">
                                    <TextBlock Text="👱‍♂️"
                                               FontSize="24"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                    
                                <Border ClipToBounds="True" CornerRadius="50">
                                    <Image Source="{Binding CurrentPhoto.Preview}"
                                           Stretch="UniformToFill"
                                           IsVisible="{Binding HasPhoto}"/>
                                </Border>
                            </Panel>
                        </Button>
                        
                        <Button Content="Удалить фото" 
                                Command="{Binding RemovePhotoCommand}"
                                CommandParameter="{Binding CurrentPhoto}"
                                IsEnabled="{Binding HasPhoto}"
                                Padding="15,8"
                                FontSize="12"
                                Background="LightCoral"
                                Foreground="White"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <StackPanel>
                            <TextBox Text="{Binding LName}" 
                                     Watermark="Фамилия" 
                                     Margin="0,5"
                                     Padding="12,8"
                                     FontSize="14"/>
                            
                            <TextBox Text="{Binding FName}" 
                                     Watermark="Имя" 
                                     Margin="0,5"
                                     Padding="12,8"
                                     FontSize="14"/>
                    
                            <TextBox Text="{Binding SName}" 
                                     Watermark="Отчество" 
                                     Margin="0,5"
                                     Padding="12,8"
                                     FontSize="14"/>
                    
                            <TextBox Text="{Binding Login}" 
                                     Watermark="Логин" 
                                     Margin="0,5"
                                     Padding="12,8"
                                     FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                            <TextBlock Text="Роль в системе:" Margin="5"/>
                            
                            <ListBox ItemsSource="{Binding Roles}" 
                                     SelectedItem="{Binding SelectedRole}" 
                                     SelectionMode="Single" 
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     VerticalAlignment="Stretch">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Value}" Margin="5"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            
                            <Button Content="Добавить сотрудника" 
                                    Command="{Binding CreateEmployeeCommand}"
                                    Background="#007ACC"
                                    Foreground="White"
                                    Padding="20,12"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Margin="0,10"
                                    CornerRadius="8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>